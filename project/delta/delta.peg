//# Gramátoca del lenguaje delta

program = expression EOF
expression = multiplicative (('+' / '-' ) multiplicative)*
primary = decimal / boolean
multiplicative = primary (('*' / '/' / '%') primary)*
decimal = r'\d+'
boolean = 'true' / 'false'

comment = r'[/][/].*|[/][*](.|\n)*?[*][/]'